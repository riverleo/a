import React, { Component } from 'react';
import styled from '../../../lib/styled';

class List extends Component {
  handle = () => {
  }

  render() {
    return (
      <div>list</div>
    );
  }
}

export default styled(List, 'UserList');
